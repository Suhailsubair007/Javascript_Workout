const fruits = ["apple","orange","banana"];
const newFruits = fruits.map((x)=>{
    return `${fruits} Roadmap`;
})

console.log(fruits);
console.log(newFruits);


//Map creates new array ..The map() method creates a new array with the results of calling a provided function on every element in the calling array. Whereas, the forEach() method executes a provided function once for each array element.